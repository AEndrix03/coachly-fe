# Test rossi accettati come baseline nota della CI.
#
# Formato: <percorso del file> :: <nome del test>
#
# Regole d'uso:
#   - non si aggiunge una riga per far passare la CI su un test che hai rotto tu;
#   - ogni test riparato esce da questo file nello stesso PR che lo ripara
#     (`tool/check_tests.sh` fallisce se un test in baseline torna verde).
#
# Stato: **vuota**. Il residuo della Fase 0 (12 test rossi per deriva fra UI e
# asserzioni, `docs/development/26-migration-plan.md`) e' stato riassorbito:
# in ogni caso aveva ragione la UI, e le asserzioni sono state riportate sul
# comportamento reale. I quattro golden sono stati rigenerati; uno di essi
# registrava lo stato collassato ed era byte per byte identico a un altro.
